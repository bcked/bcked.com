<script>
	// import { base } from '$app/paths';

	// populated with data from the endpoint
	export /** @type {any} */
	let data;

	// export async function load({ params }) {
	// 	return {
	// 		headers: { location: `${base}/assets/${data[params.address]}` },
	// 		status: 302
	// 		// redirect: { location: `${base}/assets/${data[params.address]}` }
	// 	};
	// }
</script>
